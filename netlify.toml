# The docs site is a standalone project with its own lockfile and node_modules,
# so the build runs entirely inside apps/docs and never installs the repo
# workspace. Paths below are relative to that base.
[build]
  base = "apps/docs"
  command = "pnpm build"
  publish = "dist/analog/public"
  functions = "dist/analog"
