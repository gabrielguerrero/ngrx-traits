{
  "name": "@ngrx-traits/example-api",
  "version": "0.0.0",
  "private": true,
  "description": "Mock REST API backing the example app.",
  "scripts": {
    "start": "nx serve example-api",
    "build": "nx build example-api"
  },
  "dependencies": {
    "cors": "catalog:",
    "express": "catalog:",
    "tslib": "catalog:"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.2"
  }
}
