---
max_turns: 8
allowed_tools: [Read, Glob, Grep, Skill]
---

In a plain @ngrx/signals store (no extra libraries), what is the difference between `withComputed`
and `withMethods`, and when would I use `patchState` inside a `withMethods` function?
